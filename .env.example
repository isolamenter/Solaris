# Generate with: openssl rand -base64 32
CREDENTIALS_MASTER_KEY=
# Optional. Defaults to ./.solaris-data (never reuses legacy ./data)
SOLARIS_DATA_DIR=
# Optional. Defaults to 3210. The server always binds to 127.0.0.1.
PORT=3210
